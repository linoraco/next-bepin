"use client";

import { Inter } from "next/font/google";
import "./globals.css";
import { usePathname } from "next/navigation";
import Header from "@/components/header";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

// export const metadata: Metadata = {
//   title: "EBHTPN1",
//   description: "Generated by create next app",
// };

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const pathname = usePathname();

  const linkClass =
    "hover:translate-x-1 hover:rounded-tl-2xl hover:rounded-bl-2xl text-black hover:text-black hover:bg-white duration-200 poppins-medium px-5 py-3 text-md";

  return (
    <html data-theme="corporate" lang="en">
      <body className="bg-11 text-white">
        <div className="flex-col  bg-stone-800/90 ">
          <Header />
          <div className="">{children}</div>
          <Footer />
        </div>
      </body>
    </html>
  );
}
